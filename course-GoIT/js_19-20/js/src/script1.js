jQuery(document).ready(function($) {

			//slider
			$('.my-slider').unslider({
        autoplay: true,
        arrows: false
      });

			// //hover read more
			// var first = true; // setup a global state flag
			// $("#features__span").hover( function() { // on hover
			// 	$("#features__links").css("color", first ? "green" : "red"); // toggle bgColor
			// 	$(".icon-fa-arrow-right").css("color", first ? "red" : "green"); // toggle bgColor
			// 	first = !first; // toggle flag
			// });

		});
